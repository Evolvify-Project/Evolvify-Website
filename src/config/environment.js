// Environment configuration
const environment = {
  // Base URL for the application (Vercel deployment)
  BASE_URL:
    import.meta.env.VITE_BASE_URL || "https://evolvify-website.vercel.app",
};

export default environment;
